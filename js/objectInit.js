// Object initialization function
//
function objectInit(p) {
  // Initialize axis
  xAxis.init();
  // Initialize blocks
  block1.init(1);
  block2.init(2);
  // Initialize plots
  plotXPos.init();
  plotXVel.init();
}
